{
    "name": "jarvis-root",
    "version": "1.0.0",
    "description": "Root entry point for JARVIS Vercel Deployment",
    "main": "index.js",
    "type": "module",
    "engines": {
        "node": "24.x"
    },
    "scripts": {
        "start": "node api/index.js",
        "build": "cd frontend && npm install && npm run build && mkdir -p ../public && cp -r dist/* ../public"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "dotenv": "^16.4.5",
        "express": "^4.21.2",
        "openai": "^4.77.0"
    }
}